import React from 'react';
import { LucideLayout, LucideMonitor, LucideCheckCircle, LucideAlertCircle } from 'lucide-react';

const UIShowcase = () => {
  return (
    <div className="min-h-screen bg-slate-50 text-slate-950 p-8">
      <div className="max-w-6xl mx-auto">
        <header className="mb-12">
          <h1 className="text-4xl font-black tracking-tight mb-2">Component Showcase</h1>
          <p className="text-slate-500">Live preview of almoq3 x shadcn components.</p>
        </header>

        <div className="grid md:grid-cols-2 gap-8">
          {/* Card Section */}
          <section className="p-6 bg-white border rounded-3xl shadow-sm">
            <h2 className="text-lg font-bold mb-4 flex items-center gap-2">
              <LucideLayout className="w-5 h-5 text-indigo-500" /> Buttons & States
            </h2>
            <div className="flex flex-wrap gap-4">
              <button className="px-4 py-2 bg-slate-900 text-white rounded-md font-medium hover:bg-slate-800 transition-colors">Primary</button>
              <button className="px-4 py-2 bg-white border text-slate-900 rounded-md font-medium hover:bg-slate-50 transition-colors">Outline</button>
              <button className="px-4 py-2 bg-red-500 text-white rounded-md font-medium hover:bg-red-600 transition-colors">Destructive</button>
              <button className="px-4 py-2 bg-indigo-100 text-indigo-700 rounded-md font-medium hover:bg-indigo-200 transition-colors">Secondary</button>
            </div>
          </section>

          {/* Alerts Section */}
          <section className="p-6 bg-white border rounded-3xl shadow-sm">
            <h2 className="text-lg font-bold mb-4 flex items-center gap-2">
              <LucideMonitor className="w-5 h-5 text-indigo-500" /> Alert Components
            </h2>
            <div className="space-y-4">
              <div className="p-4 bg-emerald-50 border border-emerald-200 rounded-xl flex gap-3 items-start">
                <LucideCheckCircle className="text-emerald-500 w-5 h-5 mt-0.5" />
                <div>
                  <h4 className="font-bold text-emerald-900">Success</h4>
                  <p className="text-sm text-emerald-700">The component has been installed successfully.</p>
                </div>
              </div>
              <div className="p-4 bg-amber-50 border border-amber-200 rounded-xl flex gap-3 items-start">
                <LucideAlertCircle className="text-amber-500 w-5 h-5 mt-0.5" />
                <div>
                  <h4 className="font-bold text-amber-900">Warning</h4>
                  <p className="text-sm text-amber-700">Please run npm install to sync dependencies.</p>
                </div>
              </div>
            </div>
          </section>

          {/* Progress Section */}
          <section className="p-6 bg-white border rounded-3xl shadow-sm col-span-full">
            <h2 className="text-lg font-bold mb-6">Framework Progress</h2>
            <div className="space-y-6">
              {[
                { label: "Core Library", value: "100%", color: "bg-emerald-500" },
                { label: "Auth Scaffolding", value: "100%", color: "bg-emerald-500" },
                { label: "Frontend Integration", value: "85%", color: "bg-indigo-500" }
              ].map((p, i) => (
                <div key={i}>
                  <div className="flex justify-between text-sm font-medium mb-2">
                    <span>{p.label}</span>
                    <span>{p.value}</span>
                  </div>
                  <div className="w-full h-3 bg-slate-100 rounded-full overflow-hidden">
                    <div className={`${p.color} h-full transition-all duration-1000`} style={{ width: p.value }} />
                  </div>
                </div>
              ))}
            </div>
          </section>
        </div>

        <footer className="mt-12 text-center">
          <button onClick={() => window.history.back()} className="text-indigo-600 font-bold hover:underline">
            ← Back to Dashboard
          </button>
        </footer>
      </div>
    </div>
  );
};

export default UIShowcase;
